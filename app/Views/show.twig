{% extends "default.twig" %}

{% block view %}

	<a href="/">Accueil</a>
	<h1>{{ post._titre }}</h1>
	<span>Posté le {{ post.dateAjout }}</span>
	<img src="{{ post.image }}">
	<p>{{ post.contenu }}</p>

{% endblock %}